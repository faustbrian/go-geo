schema_version: 1
tool_version: v1.0.14
tool_checksums_sha256: 88341d2f2904925b22ec8de084fec68c5229669f1119f066ed5fccd76aee2f82
manifest:
  modules: modules.json
  packages: packages.json
evidence:
  root: ".verification"
mutation:
  root: ".verification/mutation"
  imports:
  - module: "."
    archive: ".verification/mutation/bootstrap/root.zip"
    ledger: ".verification/mutation/migration-ledger.json"
api:
  baselines:
  - module: "."
    mode: apidiff
    path: api/baseline.txt
operations:
- module: "."
  gate: fuzz
  steps:
  - type: go-test
    packages: ["."]
    fuzz: '^FuzzGeometryConstructors$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./adapter/gogeom"]
    fuzz: '^FuzzFromGoGeom$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./geodesy"]
    fuzz: '^FuzzModels$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./geohash"]
    fuzz: '^FuzzDecode$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./geojson"]
    fuzz: '^FuzzDecode$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./postgis"]
    fuzz: '^FuzzValueScan$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["."]
    fuzz: '^FuzzValueDecoding$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./wkb"]
    fuzz: '^FuzzDecode$'
    budget: 10000x
    timeout: 20m
  - type: go-test
    packages: ["./wkt"]
    fuzz: '^FuzzDecode$'
    budget: 10000x
    timeout: 20m
- module: "."
  gate: benchmark
  steps:
  - type: go-test
    packages: ["./..."]
    benchmark: "."
    budget: 1s
    timeout: 20m
